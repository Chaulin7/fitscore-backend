# CVsprings backend environment variables
# Copy this file to .env and fill in the values for local development.
# On Render, set these via the Environment Variables panel.

# --- Server -----------------------------------------------------------------
PORT=3000                              # HTTP port the server listens on
LOG_LEVEL=info                         # pino level: trace|debug|info|warn|error|fatal

# --- CORS -------------------------------------------------------------------
# Comma-separated list of allowed origins. Use '*' only for development.
# Production: ALLOWED_ORIGINS=https://cvsprings.com,https://www.cvsprings.com,https://app.cvsprings.com
ALLOWED_ORIGINS=*

# --- Database ---------------------------------------------------------------
# Absolute path to the SQLite file. On Render, this must point at a mounted
# persistent disk (e.g. /var/data/audit.db); otherwise data is lost on deploy.
# DB_PATH=/var/data/audit.db

# --- Auth (Section 3 - set before enabling /api/auth/*) ---------------------
# JWT_SECRET=                         # Long random string (32+ bytes) for signing magic-link JWTs
# AUTH_COOKIE_DOMAIN=cvsprings.com    # Cookie domain for the session cookie

# --- Email provider (Section 3 - choose one) --------------------------------
# RESEND_API_KEY=                     # Resend API key for magic-link emails
# POSTMARK_API_KEY=                   # Or Postmark
# MAILGUN_API_KEY=                    # Or Mailgun
# EMAIL_FROM=no-reply@cvsprings.com

# --- Stripe (Section 3 - set before enabling /api/billing/*) ----------------
# STRIPE_SECRET_KEY=                  # sk_live_... or sk_test_...
# STRIPE_WEBHOOK_SECRET=              # whsec_... for verifying /api/webhooks/stripe
# STRIPE_PRICE_PRO=                   # price_... for Pro plan (€49/mo)
# STRIPE_PRICE_TEAM=                  # price_... for Team plan (€199/mo, 5 seats)
# BILLING_RETURN_URL=https://app.cvsprings.com/billing
